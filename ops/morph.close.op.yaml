{
  "id": "morph.close",
  "opVersion": 1,
  "metaSchemaVersion": 1,
  "since": "v1",
  "category": "morphology",
  "displayKey": "Morphological Closing",
  "input": "any",
  "auxSlots": [],
  "determinism": "Applies morphological closing (dilation followed by erosion) with the structuring element rebuilt at play time from shape+size (Rectangle packs an elementSize-by-elementSize square, Rhombus builds a radius-elementSize diamond); the materialized element Mat is never serialized. Border pixels extrapolate per the border mode and border value; deterministic on identical input.",
  "conformance": ["ops/fixtures/morph.close"],
  "params": [
    { "key": "shape", "type": "enum", "values": ["Rectangle", "Rhombus"], "default": "Rhombus", "interpolatable": true, "displayKey": "op.morph.close.param.shape.label" },
    { "key": "elementSize", "type": "int", "default": 3, "minimum": 1, "interpolatable": true, "displayKey": "op.morph.close.param.elementSize.label" },
    { "key": "border", "type": "enum", "values": ["Default", "Constant", "Replicate", "Reflect", "Wrap", "Reflect101", "Transparent", "Isolated"], "default": "Isolated", "interpolatable": true, "displayKey": "op.morph.close.param.border.label" },
    { "key": "borderValue", "type": "int", "default": 0, "interpolatable": true, "displayKey": "op.morph.close.param.borderValue.label" }
  ],
  "constraints": [],
  "dialog": { "mode": "auto" },
  "i18nKeys": ["op.morph.close.label", "op.morph.close.param.shape.label", "op.morph.close.param.elementSize.label", "op.morph.close.param.border.label", "op.morph.close.param.borderValue.label"],
  "docs": {
    "short": "Applies morphological closing with a shaped structuring element.",
    "methods": "Images were morphologically closed with JSharp morph.close."
  },
  "errorCodes": ["recipe.unknown-operation", "recipe.params-invalid"]
}
