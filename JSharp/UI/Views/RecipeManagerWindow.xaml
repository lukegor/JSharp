<Window x:Class="JSharp.UI.Views.RecipeManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:JSharp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:RecipeManagerWindowViewModel}"
        mc:Ignorable="d"
        Title="Recipes" Height="440" Width="680">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListBox Grid.Row="0" ItemsSource="{Binding Rows}" SelectedItem="{Binding SelectedRow}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal" Margin="0,3">
						<StackPanel Width="340" VerticalAlignment="Center">
							<TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
							<TextBlock Text="{Binding Detail}" Opacity="0.7"/>
						</StackPanel>
						<Button Content="Run" Command="{Binding DataContext.RunCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Margin="4,0" MinWidth="64"/>
						<Button Content="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Margin="4,0" MinWidth="64"/>
						<Button Content="Rename" Command="{Binding DataContext.RenameCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Margin="4,0" MinWidth="64"/>
						<Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Margin="4,0" MinWidth="64"/>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
			<Button Content="Run from file..." Command="{Binding OpenFromFileCommand}" Margin="0,0,8,0"/>
			<Button Content="Share selected..." Command="{Binding ShareCommand}" CommandParameter="{Binding SelectedRow}" Margin="0,0,8,0"/>
			<Button Content="Import bundle..." Command="{Binding ImportBundleCommand}" Margin="0,0,8,0"/>
			<Button Content="Refresh" Command="{Binding RefreshListCommand}"/>
		</StackPanel>
	</Grid>
</Window>
